# Nhật ký Phát triển Hệ thống - 10/05/2026

## 1. Tóm tắt các thay đổi (Changelog)

### Quản lý Danh mục & Vật tư
- **Tách biệt Vật tư & Dịch vụ**: Cập nhật Master Data để quản lý riêng biệt Phụ tùng (vật lý) và Tiền công/Dịch vụ.
- **Giao diện**: Thêm Tab "Vật tư" và "Dịch vụ" riêng biệt; bổ sung Badge màu sắc phân loại trong bảng danh mục.
- **Hình ảnh**: Hỗ trợ chụp ảnh "Trước" và "Sau" cho từng hạng mục dịch vụ (Vd: Vá vỏ).

### Quản lý Nhân sự & Phân quyền
- **Mở rộng vai trò**: Thêm vai trò **Tài xế (Driver)** và **Điều độ (Dispatcher)** vào hệ thống.
- **Linh hoạt gán việc**: Cho phép gán bất kỳ nhân sự nào (không chỉ thợ máy) vào phiếu bảo trì.
- **API Robustness**: Nâng cấp API tạo người dùng để xử lý lỗi "Email đã tồn tại" bằng cơ chế **Upsert Profile**. Nếu email đã có, hệ thống sẽ tự động cập nhật/khôi phục hồ sơ thay vì báo lỗi.

### Hiển thị & UI/UX (Đang xử lý lỗi)
- **Thay thế ID bằng Tên**: Cập nhật toàn bộ giao diện Quản trị (Bảng danh mục, Chi tiết phiếu) để hiển thị **Họ tên** thay vì mã ID (UUID).
- **Mapping thủ công**: Thêm logic "bao vây" trong form tạo phiếu để hiện "Đang tải tên..." thay vì ID khi dữ liệu chưa về kịp.

## 2. Các thay đổi Database (SQL đã thực hiện/yêu cầu)
- Cập nhật ràng buộc `profiles_role_check` để chấp nhận vai trò mới.
- Thiết lập khóa ngoại `fk_phieu_bao_tri_performer` giữa phiếu bảo trì và hồ sơ.
- **Yêu cầu quan trọng**: Cập nhật chính sách RLS cho bảng `profiles` để cho phép Admin xem tên nhân viên.

## 3. Vấn đề tồn đọng (Open Issues)
- **ĐÃ XỬ LÝ** - Lỗi hiển thị ID: Đã fix query join danh_sach_xe, hiển thị bien_so thay vì UUID.
- **ĐÃ XỬ LÝ** - Upload ảnh thật: TicketForm đã tích hợp uploadBase64Image + insert DB thực.
- **CÒN LẠI** - RLS Supabase: Cần mở policy cho Admin fetch profiles của mọi user.

---
**Ghi chú**: Mọi thay đổi code đã được Build thành công và Push lên nhánh `main`.

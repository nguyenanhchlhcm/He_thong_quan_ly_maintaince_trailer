# Work Log - 2026-05-17

## Tổng quan
Phiên làm việc tập trung xử lý các tồn đọng từ work logs trước (14/05, 15/05) và triển khai các Phase còn lại theo Master Plan.

---

## ✅ Đã hoàn thành

### 1. RLS Supabase - Mở policy bảng profiles
- **Vấn đề**: Admin/Manager không thể fetch danh sách nhân viên do policy chỉ cho phép user xem profile của chính họ.
- **Giải pháp**: Tạo migration `supabase/fix_profiles_rls.sql` thêm policy cho ADMIN/MANAGER SELECT/UPDATE tất cả profiles.
- **File**: `supabase/fix_profiles_rls.sql`

### 2. Fix Quản lý lốp xe (Assign Tire)
- **Vấn đề**: Sau khi đổi tên bảng `danh_sach_xe` → `vehicles` và cột `id_xe` → `id`, FK của `quan_ly_vo_xe` bị gãy.
- **Giải pháp**:
  - Tạo migration `supabase/fix_tire_management.sql` tái tạo FK + thêm bảng `tire_history` + trigger auto-log.
  - Cập nhật `schema.sql` đồng bộ tất cả references sang `vehicles(id)`.
  - Thêm cột `serial_photo_url`, `tread_condition_photo_url` vào schema.
- **Files**: `supabase/fix_tire_management.sql`, `supabase/schema.sql`

### 3. Analytics Dashboard nâng cấp
- **Thay đổi**:
  - Thêm biểu đồ Area Chart chi phí theo tháng (thay Bar Chart cũ).
  - Thêm Bar Chart ngang chi phí theo xe (Top 10).
  - Thêm Pie Chart chi phí theo Gara.
  - Thêm bảng xếp hạng CP/KM theo xe với màu cảnh báo (đỏ/vàng/xanh).
  - Fix tính CP/KM thật từ view `overall_cp_km`.
  - Mở rộng Excel export với 3 sheets (tháng, xe, gara).
- **Files**: `src/features/analytics/components/AnalyticsDashboard.tsx`, `supabase/analytics_views.sql`

### 4. Tab Khách hàng & Nhà cung cấp
- **Vấn đề**: Tab đã có code nhưng bị comment out, hooks trả về mảng rỗng.
- **Giải pháp**:
  - Uncomment tabs trong `master-data/page.tsx`.
  - Fix `useCustomers()`, `useSuppliers()`, `useServices()` hooks query thật từ DB.
- **Files**: `src/app/admin/master-data/page.tsx`, `src/hooks/useMasterData.ts`

### 5. Phase 10 + 11: Offline Storage & Sync Engine
- **IndexedDB**: Đã có sẵn qua `idb-keyval` trong `ticketStore.ts`.
- **Sync Engine mới**: Tạo `useSyncEngine` hook:
  - Tự động phát hiện online/offline.
  - Auto-sync pending queue khi có mạng.
  - Upload ảnh + tạo phiếu batch.
  - Progress indicator + toast notification.
- **UI Component**: Tạo `SyncStatusIndicator` hiển thị trạng thái sync.
- **Files**: `src/hooks/useSyncEngine.ts`, `src/components/SyncStatusIndicator.tsx`

### 6. Phase 12: Manager Approval Dashboard
- **Fix bug**: AdminTicketDialog query sai tên bảng `chi_tiet_phieu_bao_tri` → `chi_tiet_vat_tu_su_dung`.
- **Fix alias**: `danh_muc_vat_tu_sku` → `skus` để khớp với cấu trúc DB hiện tại.
- **Thêm cột**: `id_gara` vào bảng `phieu_bao_tri` trong schema + TicketForm insert.
- **Files**: `src/features/maintenance/components/admin/AdminTicketDialog.tsx`, `supabase/schema.sql`, `src/features/maintenance/components/mechanic/TicketForm.tsx`

---

## 📋 SQL Migrations cần chạy trên Supabase

Thứ tự ưu tiên:
1. `supabase/fix_profiles_rls.sql` - Mở policy profiles
2. `supabase/fix_tire_management.sql` - Fix FK lốp xe + tire_history
3. `supabase/analytics_views.sql` - Tạo views cho analytics

---

## 🔴 Còn tồn đọng (Low Priority)

1. **Phase 13**: Tire Lifecycle Management nâng cao (đảo lốp tự động, cảnh báo độ sâu gai)
2. **Phase 15**: Automated Alerts (cảnh báo bảo trì theo KM, GPS fraud notification)
3. **QR Code**: Quét QR định danh xe cho thợ máy
4. **Micro-animations**: Hiệu ứng chuyển UI

---

## 📝 Lưu ý

- Mọi thay đổi cần được commit lên `main` để Vercel auto-deploy.
- Sau khi deploy, chạy `NOTIFY pgrst, 'reload schema';` trong Supabase SQL Editor để PostgREST nhận schema mới.
- Sync Engine hoạt động dựa vào `idb-keyval` - dữ liệu offline được lưu trong IndexedDB của trình duyệt.
